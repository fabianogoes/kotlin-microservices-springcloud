spring:
  livereload:
    enabled: true

logging:
  file:
    NAME: logs/configserver.log
  pattern:
    console: "%d %-5level %logger : %msg%n"
    file: "%d %-5level [%thread] %logger : %msg%n"
  level:
    root: INFO
    org:
      springframework:
        web: INFO

